---
description: Backup the project to GitHub
---

1. Stage all changes
   `git add .`

2. Commit changes with a timestamp
   `git commit -m "Backup: $(date '+%Y-%m-%d %H:%M:%S')"`

3. Push to remote
   `git push`
